{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "\\.cs$",
        "cmd": "dotnet format whitespace ./DotnetAnalyzyHabr.sln --include ${relativeFile} --verbosity diagnostic"
      }
    ]
  }
}
